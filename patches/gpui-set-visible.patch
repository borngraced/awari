diff --git a/crates/gpui/src/platform.rs b/crates/gpui/src/platform.rs
index ab4b500..15e115a 100644
--- a/crates/gpui/src/platform.rs
+++ b/crates/gpui/src/platform.rs
@@ -846,6 +846,12 @@ pub trait PlatformWindow: HasWindowHandle + HasDisplayHandle {
     fn zoom(&self);
     fn toggle_fullscreen(&self);
     fn is_fullscreen(&self) -> bool;
+    /// Adjust layer-shell keyboard focus policy at runtime.
+    ///
+    /// No-op by default. Implemented for Wayland layer-shell surfaces so an
+    /// always-mapped overlay can take Exclusive focus while open and release
+    /// it (None) when dismissed.
+    fn set_keyboard_interactivity(&self, _interactivity: crate::layer_shell::KeyboardInteractivity) {}
     fn frame_waker(&self) -> Option<Rc<dyn Fn()>> {
         None
     }
diff --git a/crates/gpui/src/window.rs b/crates/gpui/src/window.rs
index 50d3d91..549a06c 100644
--- a/crates/gpui/src/window.rs
+++ b/crates/gpui/src/window.rs
@@ -2035,6 +2035,17 @@ impl Window {
             .and_then(|id| FocusHandle::for_id(id, &cx.focus_handles))
     }
 
+    /// Adjust the layer-shell keyboard focus policy at runtime.
+    ///
+    /// No-op except for Wayland layer-shell surfaces. Lets an always-mapped
+    /// overlay take `Exclusive` focus while open and release it with `None`.
+    pub fn set_keyboard_interactivity(
+        &self,
+        interactivity: crate::layer_shell::KeyboardInteractivity,
+    ) {
+        self.platform_window.set_keyboard_interactivity(interactivity);
+    }
+
     /// While focus-lost listeners are being dispatched, returns the closest ancestor of the
     /// previously focused element that can still receive focus, making it a suitable target
     /// for focus restoration. Returns `None` at all other times, or when no such ancestor exists.
diff --git a/crates/gpui_linux/src/linux/wayland/window.rs b/crates/gpui_linux/src/linux/wayland/window.rs
index eb84b7f..f28dbeb 100644
--- a/crates/gpui_linux/src/linux/wayland/window.rs
+++ b/crates/gpui_linux/src/linux/wayland/window.rs
@@ -38,7 +38,7 @@ use gpui::{
     PlatformWindow, Point, PromptButton, PromptLevel, RequestFrameOptions, ResizeEdge, Scene, Size,
     Tiling, WindowAppearance, WindowBackgroundAppearance, WindowBounds, WindowControlArea,
     WindowControls, WindowDecorations, WindowKind, WindowParams,
-    layer_shell::{Anchor, LayerShellNotSupportedError},
+    layer_shell::{Anchor, KeyboardInteractivity, LayerShellNotSupportedError},
     popup::PopupOptions,
     px, size,
 };
@@ -1829,6 +1829,17 @@ impl PlatformWindow for WaylandWindow {
         }
     }
 
+    fn set_keyboard_interactivity(&self, interactivity: KeyboardInteractivity) {
+        let state = self.borrow();
+        if let WaylandSurfaceState::LayerShell(ls) = &state.surface_state {
+            ls.layer_surface
+                .set_keyboard_interactivity(super::layer_shell::wayland_keyboard_interactivity(
+                    interactivity,
+                ));
+            state.surface.commit();
+        }
+    }
+
     fn zoom(&self) {
         let state = self.borrow();
         if let Some(toplevel) = state.surface_state.toplevel() {
